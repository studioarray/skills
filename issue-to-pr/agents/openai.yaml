interface:
  display_name: "Issue to PR"
  short_description: "Implement GitHub issues through reviewed pull requests"
  default_prompt: "Use $issue-to-pr to implement this GitHub issue and its sub-issues through a reviewed pull request: <GitHub issue URL>."
policy:
  allow_implicit_invocation: true
